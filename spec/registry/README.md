# Protocol Registry

`protocol-registry.json` is the frozen registry for protocol-visible identifiers in v0.1.

Workers must not create aliases or parallel identifiers for existing semantics. New protocol-visible names require governed registry evolution.
